/**
 * Product constants that the specification still marks as TBD.
 * They are provisional placeholders, kept in one place so they can be changed
 * without touching feature logic.
 */

/** Spec §8: "初期評価本数は 10 を仮置き". */
export const ONBOARDING_TARGET_RATINGS = 10;

/** How many titles the recommendation engine scores per request. */
export const RECOMMENDATION_POOL_SIZE = 24;
